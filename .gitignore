# Built application files
build/
release/
*.apk
*.zip
.externalNativeBuild
.cxx

# NOTE: proot binaries under app/src/main/jniLibs/**/*.so MUST be committed,
# so we deliberately do NOT ignore *.so here.

# Local configuration file (sdk path, etc)
local.properties

# Gradle generated files
.gradle/

# IDE
.idea/
*.iml
*.swo
*.swp

# Keystore (use GitHub Secrets in CI, NOT committed)
prootz-release.jks

# OS-specific files
.DS_Store
Thumbs.db
